const axios = require('axios');

async function obtenerUrlAleatoria(url) {
    try {
        const response = await axios.get(url);
        return response.data.url;
    } catch (error) {
        throw new Error(`Error al obtener la URL aleatoria: ${error.message}`);
    }
}

async function wantwaifuxsfw() {
    const url = 'https://api.waifu.pics/sfw/waifu';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de waifu: ${error.message}`);
    }
}

async function wantnekoxsfw() {
    const url = 'https://api.waifu.pics/sfw/neko';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de neko: ${error.message}`);
    }
}

async function wantshinobuxsfw() {
    const url = 'https://api.waifu.pics/sfw/shinobu';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de shinobu: ${error.message}`);
    }
}

async function wantmeguminxsfw() {
    const url = 'https://api.waifu.pics/sfw/megumin';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de megumin: ${error.message}`);
    }
}

async function wantbullyxsfw() {
    const url = 'https://api.waifu.pics/sfw/bully';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de bully: ${error.message}`);
    }
}

async function wantcuddlexsfw() {
    const url = 'https://api.waifu.pics/sfw/cuddle';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de cuddle: ${error.message}`);
    }
}

async function wantcryxsfw() {
    const url = 'https://api.waifu.pics/sfw/cry';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de cry: ${error.message}`);
    }
}

async function wanthugxsfw() {
    const url = 'https://api.waifu.pics/sfw/hug';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de hug: ${error.message}`);
    }
}

async function wantawooxsfw() {
    const url = 'https://api.waifu.pics/sfw/awoo';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de awoo: ${error.message}`);
    }
}

async function wantkissxsfw() {
    const url = 'https://api.waifu.pics/sfw/kiss';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de kiss: ${error.message}`);
    }
}

async function wantlickxsfw() {
    const url = 'https://api.waifu.pics/sfw/lick';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de lick: ${error.message}`);
    }
}

async function wantpatxsfw() {
    const url = 'https://api.waifu.pics/sfw/pat';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de pat: ${error.message}`);
    }
}

async function wantsmugxsfw() {
    const url = 'https://api.waifu.pics/sfw/smug';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de smug: ${error.message}`);
    }
}

async function wantbonkxsfw() {
    const url = 'https://api.waifu.pics/sfw/bonk';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de bonk: ${error.message}`);
    }
}

async function wantyeetxsfw() {
    const url = 'https://api.waifu.pics/sfw/yeet';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de yeet: ${error.message}`);
    }
}

async function wantslapxsfw() {
    const url = 'https://api.waifu.pics/sfw/slap';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de slap: ${error.message}`);
    }
}

async function wantkillxsfw() {
    const url = 'https://api.waifu.pics/sfw/kill';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de kill: ${error.message}`);
    }
}

async function wantkickxsfw() {
    const url = 'https://api.waifu.pics/sfw/kick';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de kick: ${error.message}`);
    }
}

async function wanthappyxsfw() {
    const url = 'https://api.waifu.pics/sfw/happy';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de happy: ${error.message}`);
    }
}

async function wantdancexsfw() {
    const url = 'https://api.waifu.pics/sfw/dance';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de dance: ${error.message}`);
    }
}

async function wantcringexsfw() {
    const url = 'https://api.waifu.pics/sfw/cringe';
    try {
        const imageUrl = await obtenerUrlAleatoria(url);
        return imageUrl;
    } catch (error) {
        throw new Error(`Error al obtener la imagen de cringe: ${error.message}`);
    }
}

module.exports = {
    wantwaifuxsfw,
    wantnekoxsfw,
    wantshinobuxsfw,
    wantmeguminxsfw,
    wantbullyxsfw,
    wantcuddlexsfw,
    wantcryxsfw,
    wanthugxsfw,
    wantawooxsfw,
    wantkissxsfw,
    wantlickxsfw,
    wantpatxsfw,
    wantsmugxsfw,
    wantbonkxsfw,
    wantyeetxsfw,
    wantslapxsfw,
    wantkillxsfw,
    wantkickxsfw,
    wanthappyxsfw,
    wantdancexsfw,
    wantcringexsfw
};